name: Bandit

on:
  push:
    branches: [main]
permissions:
  security-events: write
  # only required for workflows in private repositories
  actions: read
  contents: read

jobs:
  analyze:
    runs-on:
      ubuntu-latest
      # required for all workflows
    steps:
      - name: Perform Bandit Analysis
        uses: PyCQA/bandit-action@v1.0.1
# - name: Bandit by PyCQA
#   # You may pin to the exact commit or the version.
#   # uses: PyCQA/bandit-action@67a458d90fa11fb1463e91e7f4c8f068b5863c7f
#   uses: PyCQA/bandit-action@v1.0.1
#   with:
#     # Optional config file to use for selecting plugins and overriding defaults

#     configfile: default # optional, default is DEFAULT
#     # Profile to use (defaults to executing all tests)

#     profile: default # optional, default is DEFAULT
#     # Comma-separated list of test IDs to run

#     tests: # optional, default is DEFAULT
#     # Comma-separated list of test IDs to skip

#     skips: default # optional, default is DEFAULT
#     # Report only issues of a given severity level or higher. "all" and "low"
# #are likely to produce the same results, but it is possible for rules to
#     # be undefined which will not be listed in "low". Options include:
#     #   {all, high, medium, low}

#     severity: default # optional, default is DEFAULT
#     # Report only issues of a given confidence level or higher. "all" and "low"
# # are likely to produce the same results, but it is possible for rules to
# # be undefined which will not be listed in "low". Options include:
# # {all, high, medium, low}

#     confidence: default # optional, default is DEFAULT
#     # Comma-separated list of paths (glob patterns supported) to exclude from
# # scan (note that these are in addition to the excluded paths provided in
# # the config file)

#     exclude: default # optional, default is .svn,CVS,.bzr,.hg,.git,__pycache__,.tox,.eggs,*.egg
#     # Path of a baseline report to compare against (only JSON-formatted files
# # are accepted)

#     baseline: default # optional, default is DEFAULT
#     # Path to a .bandit file that supplies command line arguments

#     ini: default # optional, default is DEFAULT
#     # Source file(s) or directory(s) to be tested

#     targets: default # default is .
